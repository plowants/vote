namespace Vote.Model.Enums
{
    /// <summary>
    /// 支付状态：0未支付，1.支付中，2支付成功，3支付失败
    /// </summary>
    public enum EPayStatus
    {
        未支付 = 0,
        支付中 = 1,
        支付成功 = 2,
        支付失败 = 3
    }
}